# --- Core HF stack ---
transformers>=4.40
tokenizers>=0.15
safetensors

# Data / utils
numpy>=1.24
tqdm>=4.66
datasets>=2.16

# Logging / monitoring
tensorboard>=2.12
psutil>=5.9
nvidia-ml-py3        # NVML bindings (pynvml) for power/usage monitor

# Plotting (for plotter.py)
matplotlib>=3.7

# Optional extras (uncomment only if you really use them)
gitpython
scipy
